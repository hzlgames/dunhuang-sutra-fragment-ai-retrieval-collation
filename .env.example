# =====================================
# 敦煌经书残卷 AI 检索系统 - 环境配置示例
# =====================================

# GCP 项目 ID（在 GCP Console 中查看）
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# GCP 区域（建议使用 global 以支持 Gemini 3 Pro）
GOOGLE_CLOUD_LOCATION=global

# Service Account 凭据文件路径（下载 JSON 密钥文件到项目根目录）
GOOGLE_APPLICATION_CREDENTIALS=service-account-key.json

# Google Cloud Storage 存储桶名称（批处理模式需要，可选）
VERTEX_BATCH_BUCKET=

# 输出目录（可选，默认：output）
OUTPUT_DIR=output

# 会话记录目录（可选，默认：sessions）
SESSIONS_DIR=sessions
